Задачи:
    1) Давать доступ к инфе о сотрудниках (добавление и удаление).
    2) Давать возможность публиковать и оценивать статьи.
    3) Давать возможность авторизоваться.

Тут же задачи модуля App:
    1) Навигация по сайту. Подгрузка данных страниц лежит на нем.
     Все взаимодействия с меню в шапке и подвале также лежат на нем.
    2) История обрабатываться будет в каждом компоненте, где это будет необходимо.
     В компоненте App будет изменяться адрес страницы и записываться текущее состояние компонента.
     Состояние других компонентов он учитывать не будет!
     При переходах по истории(вперед/назад) изменения будут обрабатываться в каждом компоненте отдельно.

     // Чет читаю и думаю, что ничерта не понятно...
     // Да и фиг с ним. Потом допишем
